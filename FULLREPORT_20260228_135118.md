# FULL REPORT - Application General Checkup

Report Date/Time (Pakistan Standard Time): 2026-02-28 13:50:22 PKT
Repository: `e:\WORKINGS\current working\pware`
Audit Type: Static code audit + command validation + prior report reconciliation

## 0) Previous Reports Reviewed
- `FIX_REPORT.md`
- `FIX_REPORT_DAILY_TRANSACTIONS.md`
- `AUDIT_REPORT_FULL.md`
- `PLAN-B.md`

### Previous Fixes Re-Check
- Import invalid/missing dispatch dates being skipped: FOUND in code (`blueprints/import_export.py:578-579`) -> FIX PRESENT.
- Daily transactions backend filters (material/bill/client): FOUND in code (`blueprints/inventory.py:208-227`) -> FIX PRESENT.
- Several old security/findings from `AUDIT_REPORT_FULL.md`: PARTIALLY fixed (many delete routes now POST), but critical gaps still remain (details below).

## 1) Rewritten Master Prompt (Same Point System)
1. Each delivery time must be tracked and visible.
2. Each delivery date must be tracked and visible.
3. Each delivery stock must be validated against the client.
4. Each delivery stock must be reflected in summary.
5. Each delivery stock must be reflected in invoice.
6. Each delivery stock price must be reflected in invoice.
7. Each delivery stock balance must be reflected in summary.
8. Each delivery must be trackable across: booking, booking delivery, cash sale, billed, unbilled, registered clients, client ledger, unregistered clients, open khata.
9. Each delivery must appear in daily breakdown.
10. Each delivery must be schema-accurate.
11. Each report must be schema-accurate.
12. Schema must be accurate for all modules and functions.
13. Schema must be accurate for import/export.
14. Schema must be accurate for DB backup/restore.
15. Import/export must include all modules and data updates.
16. DB backup/restore must support current tenant only, with no root-account side effects.
17. Root account data backup/restore must function correctly.
18. Profit/loss must be accurate for each delivery impact (cash/credit impact; booking delivery already impacted).
19. Tracking and filters must work across modules by date range, client, material.
20. If all materials are suspended, there must be an activate-all path; same check for suspended clients.
21. All modules must remain linked/synced in real time (clients, ledgers, sales, bookings, booking delivery, GRN, cancellations, profit/loss, stock summary, payments, decision ledger, pending amounts/bills, credit sales, open khata, financial ledger, materials ledger, void, import).

## 2) Validation Commands Run
- `python -m compileall -q .` -> PASS (`COMPILE_OK`)
- `python test_modules.py` -> FAIL (Windows console encoding issue; cp1252 cannot print unicode box chars)
- `python -m pytest -q` -> FAIL (`pytest` missing)
- `python analyze_db.py` -> FAIL (hardcoded invalid db path)
- `python check_duplicates.py` -> FAIL (hardcoded invalid db path)
- `python investigate_bills.py` -> FAIL (hardcoded invalid db path)

## 3) Point-by-Point Status (1-21)
1. PASS - Delivery time exists and is shown (`Entry.time`, `tracking.html`, `daily_transactions.html`).
2. PASS - Delivery date exists and is shown (`Entry.date`, filters/pages).
3. PARTIAL - Client/booking validations exist in sales flow, but not fully uniform across all import/reconciliation paths.
4. PASS - Stock summary route and totals by material present (`blueprints/inventory.py`).
5. PASS - Invoice/bill view contains material delivery summary (`templates/view_bill.html:182-205`).
6. PASS - Invoice table shows rate and amount per item (`templates/view_bill.html:97-110`).
7. PASS - Stock balances shown in summary (`stock_summary` totals and net).
8. PARTIAL - Tracking exists for billed/unbilled/open-khata and categories, but registered vs unregistered view is not fully explicit as a single unified tracker.
9. PASS - Daily breakdown route and page exist (`/inventory/daily_transactions`).
10. PARTIAL - Data Lab still writes invalid model fields, creating schema mismatch risk (`blueprints/data_lab.py:140,145`).
11. PARTIAL - Reporting works broadly, but supplier payment invoice presentation has negative pending line artifact.
12. PARTIAL - Core schema works, but mixed datetime typing and plaintext-password field remain technical debt/risk.
13. PARTIAL - Import/export is strong, but master import/export does not cover every module table symmetrically.
14. PARTIAL - Backup/restore exists; tenant replace mode safe for non-root, but full-raw export still includes non-tenant tables without scoping if table lacks `tenant_id`.
15. PARTIAL - Not all modules/tables are included in joint master import/export pipeline.
16. PASS - Tenant replace mode restricted by `tenant_id`; root replace mode blocked in full raw import.
17. PARTIAL - Root has recovery and upgrade tooling; needs explicit tested root backup/restore workflow evidence in automation.
18. PARTIAL - Profit/loss code exists and computes by transactions, but no executable test suite currently verifying edge-case accuracy.
19. PARTIAL - Filters exist in multiple modules, but consistency is incomplete and some UI action wiring is broken.
20. FAIL - No dedicated "Activate All" button for all suspended materials/clients; only per-record restore paths.
21. PARTIAL - Reconciliation exists (`/reconcile_data`) but real-time linkage is weakened by broken UI actions and Data Lab schema bugs.

## 4) High-Priority Current Errors Found

### A) Broken UI Actions (POST routes linked with GET anchors)
The backend routes are POST-only, but templates still call them via `<a href=...>` (GET), causing 405/runtime breakage.
Examples:
- `templates/bookings.html:94` -> `/delete_bill/Booking/...`
- `templates/direct_sales.html:438` -> `/delete_bill/DirectSale/...`
- `templates/payments.html:94` -> `/delete_bill/Payment/...`
- `templates/pending_bills.html:150` -> `/delete_pending_bill/...`
- `templates/tracking.html:392` and `templates/daily_transactions.html:260` -> `/delete_entry/...`
- `templates/materials.html:186` -> `/delete_material/...`
- `templates/clients.html:129` -> `/client_toggle_active/...`
- `templates/settings.html:196` -> `/delete_user/...`

### B) Data Lab Schema Breaks
- `blueprints/data_lab.py:140` uses `Entry(... client_name=...)` but `Entry` model field is `client`.
- `blueprints/data_lab.py:145` uses `PendingBill(... date=...)` but model has `created_at`.
- Also uses `datetime.utcnow()` instead of Pakistan-local app time.

### C) Supplier Payment Invoice Bottom Pending Negative Artifact
- Supplier payment PDF uses generic `view_bill.html` with `type='Payment'` and no client context (`main.py:9256-9267`).
- Invoice footer prints "This Bill Pending" as negative for payment (`templates/view_bill.html:168-171`).
- This matches your complaint.

### D) Timezone Consistency Gaps
- Main app uses PKT helpers (`main.py:60-70`), good.
- Blueprint imports still use `datetime.now()`/`datetime.utcnow()` in many places (`blueprints/import_export.py`, `blueprints/data_lab.py`, `blueprints/admin.py`) causing mixed timestamps.

### E) Audit/Utility Scripts Not Usable in Current Repo
- `analyze_db.py`, `check_duplicates.py`, `investigate_bills.py` point to hardcoded linux path (`/home/claude/rv5/...`) and fail locally.

## 5) Supplier Ledger Requirement Check
Requirement: Supplier ledger should show 3 amount columns like client/financial ledger (`TOTAL`, `PAID`, `BALANCE`).

Current:
- Transaction table already has equivalent per-row columns (`Credit`, `Debit`, `Balance`) in `templates/supplier_ledger.html:41-43`.
- Missing: explicit top summary trio cards (`TOTAL`, `PAID`, `BALANCE`). Only current balance card is present.
Status: PARTIAL (functional but not in requested exact format).

## 6) Backup/Restore Requirement Check
- Tenant backup/export available from Settings and Import/Export center.
- Tenant restore (`replace_tenant_data`) restricted for non-root and blocked for root in full raw import (`blueprints/import_export.py:2184-2186`).
- Root has app-upgrade + rollback and master import/export routes.
Status: PASS/PARTIAL mix (core logic present, but needs full regression tests and hardening for non-tenant tables).

## 7) Full Data Validation + Syntax Validation
- Syntax compilation passes.
- Automated behavioral validation is incomplete due missing `pytest` and script failures.
Status: PARTIAL.

## 8) Recommended Fix Set (Pending Your Approval)
1. Convert all remaining destructive/toggle action anchors to POST forms (critical runtime fix).
2. Fix Data Lab model field mismatches + PKT timestamps.
3. Fix supplier payment invoice footer logic to avoid negative pending display.
4. Add supplier ledger summary trio: `Total`, `Paid`, `Balance`.
5. Add "Activate All Suspended" actions for materials and clients.
6. Normalize import/export/admin timestamps to PKT helper.
7. Repair utility scripts to use repo-local DB path (`instance/ahmed_cement.db`).
8. Add lightweight regression checks for reconciliation, filters, and invoice math.

## 9) Final Audit Conclusion
The application is feature-rich and mostly functional, but not yet fully compliant with your full checklist due several concrete regressions and partial implementations. The most urgent blockers are broken POST action wiring in templates, Data Lab schema mismatches, and supplier payment invoice pending display behavior.
